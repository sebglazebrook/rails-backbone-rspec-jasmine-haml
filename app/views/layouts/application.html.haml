!!!
%html{:lang => 'en'}
    %head
        %title Appy
        = stylesheet_link_tag "application", :media => "all"
        = javascript_include_tag "application"
        = csrf_meta_tags

    %body

        = yield

    :javascript
        $(document).ready(function() {
            Appy.init({});
        });
